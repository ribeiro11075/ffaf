version: '3.3'

services:
  server:
    build: .
    image: server
    command: npm run dev
    container_name: ffaf-server
    ports:
      - "8080:8080"
    volumes:
      - ./:/usr/src/app
      - /usr/src/app/node_modules
